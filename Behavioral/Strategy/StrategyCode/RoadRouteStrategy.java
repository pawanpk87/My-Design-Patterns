package StrategyCode;

public class RoadRouteStrategy implements RouteStrategy{
    @Override
    public void buildRoute(String source, String destination) {
        System.out.println("Building road route from " + source + " to " + destination);
        // Implement road route algorithm
    }
}